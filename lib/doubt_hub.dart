import 'package:flutter/material.dart';

class DoubtHub extends StatelessWidget {
  const DoubtHub({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Doubt Hub'),
      ),
      body: const Center(
        child: Text('Doubt Hub'),
      ),
    );
  }
}
